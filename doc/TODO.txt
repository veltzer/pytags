core
====
- do the walking and the scanning.
	make the walking insert the tags into the database if they are missing.
	load all the tag names at the begining from the database so that I will know which tags exist
	and which do not.

misc
====
add command line autocompletion bash for this package.
do a manual page for this tool.

build system
============
make a package out of this and put it up on my site (install it on my laptop).
